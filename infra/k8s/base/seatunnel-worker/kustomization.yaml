apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

configMapGenerator:
  - name: seatunnel-worker-config
    files:
      - hazelcast.yaml
      - seatunnel.yaml
  - name: seatunnel-log4j-config
    files:
      - log4j.properties
