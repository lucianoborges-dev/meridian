apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/seatunnel-master

patches:
  - path: patch-deployment.yaml

configMapGenerator:
  - name: seatunnel-master-config
    behavior: replace
    files:
      - hazelcast.yaml
      - hazelcast-client.yaml
      - seatunnel.yaml
  - name: seatunnel-log4j-config
    behavior: replace
    files:
      - log4j.properties      