apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/cassandra

configMapGenerator:
  - name: cassandra-config
    behavior: create
    files:
      - cassandra.yaml
